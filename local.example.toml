# Optional troubleshooting configuration. Not a Codex config file.
# Copy outside this repository or use the ignored local.toml filename.
schema_version = 1

# Define only external skill paths that you explicitly want doctor to inspect.
# Leaving these sections unset is normal and does not produce a warning.

# [superpowers]
# skills_root = "/absolute/path/to/plugin/skills"

# [git_commit]
# skill_file = "/absolute/path/to/git-commit/SKILL.md"

# Windows paths should use TOML literal strings or escaped backslashes.
